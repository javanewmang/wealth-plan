server.port=8080
app.cors.allowed-origin=http://localhost:5173

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/wealth_plan}
spring.datasource.username=${DB_USERNAME:wealth}
spring.datasource.password=${DB_PASSWORD:wealth}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.sql.init.mode=always

app.ws.topic.alerts=/topic/alerts
